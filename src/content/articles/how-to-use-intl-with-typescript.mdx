---
title: How to use Intl.DateTimeFormat with typescript
date: 2024-07-06T01:00
tags: js, Intl.DateTimeFormat, typescript
overview: sometime we need to transform data from programing format to user readeble format. But we need some problem with typescript when we use intl object.
previewImage: /images/articles-images/svg-sprite-banner.jpg
previewImageAlt: rondo przed wejściem głównym do dworca
isRecent: true
---

import ContentPart from '../../markdown-components/content-part/content-part.astro';
import ArticleParagraphM from '../../markdown-components/typography/article-paragraph-m.astro';
import ArticleTitleL from '../../markdown-components/typography/article-title-l.astro';

<ContentPart>
	<ArticleTitleL headingTag="h2">how we usually use intl?</ArticleTitleL>
	<ArticleParagraphM>
		For first, we must understand what is a svg sprite. “An SVG sprite is a single SVG file that
		contains multiple icons as &ltsvg&gt elements. Each icon has a unique id attribute that you can
		use to select it.”. For example, I show for you my svg sprite. For first, we need to create a
		separate svg file. Usually, I create this file in images folder. That file must looks like this
	</ArticleParagraphM>
|

```javascript
const options = {
	year: 'numeric',
	month: 'short',
	day: '2-digit',
	hour: '2-digit',
	minute: '2-digit',
	timeZone: 'CET',
	hour12: false,
};

const formattedDate = new Intl.DateTimeFormat('en-GB', options).format(d);
```

</ContentPart>
