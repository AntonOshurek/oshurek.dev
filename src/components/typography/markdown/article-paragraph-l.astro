<div class="article-l-paragraph">
	<slot />
</div>

<style>
	@import '../../../styles/variables/variables.scss';
	.article-l-paragraph {
		font-family: var(--article-paragraph-font);
		font-weight: var(--l-text-weight);
		font-size: var(--l-text-size);
		line-height: var(--l-text-line-height);
		color: var(--primary-text);

		@media (max-width: $mobile-width) {
			font-weight: var(--m-text-weight);
			font-size: var(--m-text-size);
			line-height: var(--m-text-line-height);
		}
	}
</style>
