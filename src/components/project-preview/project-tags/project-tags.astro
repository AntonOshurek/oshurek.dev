---
interface Props {
	tags: string;
}

const { tags } = Astro.props;

const cleanStr = tags.replace(/tags:|\s|,|-/g, ' ').trim();
const wordsArray = cleanStr.split(/\s+/).filter(Boolean);
---

<div class="project-preview__tags">
	{
		wordsArray.map((tag) => {
			return <h4 class="project-preview__tag project-tags">{tag}</h4>;
		})
	}
</div>
